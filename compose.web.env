# Loaded only by the `web` service (see docker-compose.prod.yml). No API secrets here.
# Omit this file key entirely to allow the route; set false / 0 / no / off to return 404 for /external-api-playground.
EXTERNAL_PLAYGROUND_ENABLED=true
